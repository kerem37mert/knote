import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
        backgroundColor: "blue"
    },

    title: {
        fontSize: 32,
        fontWeight: "bold"
    },

    note: {
        backgroundColor: "red",
        color: "black",
        fontSize: 16
    }
});